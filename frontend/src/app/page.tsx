export default function Home() {
  return (
      <main className="mx-auto max-w-5xl px-4 py-10">
        <h1 className="text-3xl font-semibold">Next.js + Spring Boot E-Shop</h1>
        <p className="mt-2 text-muted-foreground">
          Frontend ready. Next step: layout + navbar + products.
        </p>
      </main>
  );
}